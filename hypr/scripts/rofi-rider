#!/usr/bin/env nu

let results = fd -t f -e sln . $env.CODE

if ($results | is-empty) {
    notify-send "no solutions found"
    return
}

let selected = $results | rofi -dmenu -p "select solution"

if ($selected | is-empty) {
    notify-send "no solution selected"
    return
}

rider $selected
